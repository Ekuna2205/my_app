name: awto
description: "Авто угаалгын цаг захиалгын апп"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Календарь харуулахад (table calendar)
  table_calendar: ^3.1.2

  # Огноо, цагийг зөв форматлахад
  intl: ^0.19.0

  # Firebase (хэрэглэхээр бол, одоогоор тайлбарлаж байна)
  # firebase_core: ^3.6.0
  # cloud_firestore: ^5.4.4
  # firebase_auth: ^5.3.1

  # Хэрэв Riverpod ашиглах бол (state management-д сайн)
  # flutter_riverpod: ^2.5.1
  # riverpod_annotation: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Хэрэв зураг, лого гэх мэт нэмэх бол энд оруулна
  # assets:
  #   - assets/images/